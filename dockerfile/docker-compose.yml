version: '3.9'
services:
  inventory_sync:
    image: postgres:16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: inventory_sync
    ports:
      - "5432:5432"

    volumes:
      - ./data:/var/lib/postgresql/data
volumes:
  postgres_data: